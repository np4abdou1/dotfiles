background {
  monitor =
  path = screenshot
  blur_passes = 3
  blur_size = 8
}

input-field {
  monitor =
  size = 250, 50
  outline_thickness = 2
  dots_size = 0.2
  dots_spacing = 0.5
  dots_center = true
  outer_color = rgba(4488ffaa)
  inner_color = rgba(111111ee)
  font_color = rgba(ccccccff)
  fade_on_empty = true
  placeholder_text = <i>Password...</i>
  hide_input = false
  position = 0, -80
  halign = center
  valign = center
}

label {
  monitor =
  text = $TIME
  font_size = 48
  color = rgba(ccccccff)
  position = 0, 40
  halign = center
  valign = center
}

label {
  monitor =
  text = Hi there, locked!
  font_size = 14
  color = rgba(888888ff)
  position = 0, -10
  halign = center
  valign = center
}
